##Question 1

--Convert the funding_total_usd and founded_at_clean columns 
--in the tutorial.crunchbase_companies_clean_date table to strings (varchar format) 
--using a different formatting function for each one.

select CAST(funding_total_usd AS varchar) as funding_total_usd,
CAST(founded_at_clean AS varchar) as founded_at_clean
from tutorial.crunchbase_companies_clean_date ;
